# Optional: enables real GPT-5.6 explanations. Never commit your real key.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-terra

# Real autonomous-agent runs use the OpenAI Agents SDK.
# Start one through POST /api/agent-runs after installing backend requirements.
